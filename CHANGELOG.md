# 更新日志

## [1.1.3] - 2025-10-02

### ✨ 功能增强：日期切换动画

#### 改进内容
- ✅ **添加日期切换动画**：点击日期时带有平滑的弹性动画效果
- ✅ **背景色过渡**：红色圆形背景出现/消失带有 spring 动画
- ✅ **文字颜色过渡**：文字颜色和字重变化更加流畅自然
- ✅ **提升用户体验**：动画让交互反馈更加明显和愉悦

#### 技术细节
```swift
// 按钮点击时的动画包裹
Button(action: {
    withAnimation(.spring(response: 0.3, dampingFraction: 0.7)) {
        onDateSelected(day.date)
    }
})

// UI 状态变化的动画
.animation(.spring(response: 0.3, dampingFraction: 0.7), value: isSelected)
```

**动画参数**：
- `response: 0.3` - 动画持续时间 300ms
- `dampingFraction: 0.7` - 阻尼系数，带有轻微弹性效果
- 类型：Spring（弹性动画）- 符合 iOS 设计规范

#### 影响范围
- 修改文件：`IdeaBox/Views/WeekCalendarView.swift`
- 代码变更：2 行（添加动画修饰符）
- 性能影响：无（SwiftUI 原生动画优化）

---

## [1.1.2] - 2025-10-02

### 🐛 Bug 修复："今天"按钮日历滚动问题

#### 问题描述
点击"今天"按钮后，日期虽然选中了今天，但顶部的周日历没有自动滚动到今天所在的周，用户需要手动滑动才能看到。

#### 修复内容
- ✅ 添加了 `selectedDate` 的监听逻辑，当日期变化时自动滚动日历
- ✅ 引入 `isUpdatingFromSwipe` 标志，区分手动滑动和程序更新
- ✅ 避免循环更新：手动滑动翻周时不触发日历滚动
- ✅ 点击"今天"按钮时带有平滑动画滚动到今天所在周

#### 技术细节
```swift
// 新增状态标志
@State private var isUpdatingFromSwipe: Bool = false

// 监听日期变化并自动滚动
.onChange(of: selectedDate) { newDate in
    guard !isUpdatingFromSwipe else { return }
    let targetOffset = dateHelper.weekOffset(for: newDate, relativeTo: Date())
    if targetOffset != currentWeekOffset {
        withAnimation {
            currentWeekOffset = targetOffset
        }
    }
}
```

#### 影响范围
- 修改文件：`IdeaBox/Views/WeekCalendarView.swift`
- 代码变更：15 行（新增逻辑）
- 性能影响：无

---

## [1.1.1] - 2025-10-02

### 🎨 UI 优化：日历布局改进

#### 改进内容
- ✅ **大幅减少留白区域**：整体高度从 100pt 降低到 60pt，更加紧凑
- ✅ **优化顶部间距**：顶部 padding 从 54pt → 16pt → 8pt，三次优化
- ✅ **日期均匀分布**：7个日期按钮在屏幕宽度上均匀分布，自适应不同屏幕尺寸
- ✅ **调整左右边距**：从 12pt 增加到 16pt，视觉更加平衡

#### 技术细节
- 使用 SwiftUI 的 `.frame(maxWidth: .infinity)` 实现响应式布局
- 移除固定的 `spacing: 8`，改用均匀分布
- 整体高度优化：100pt → 60pt（减少 40%）
- 顶部内边距优化：54pt → 8pt（减少 85%）
- 保持所有功能不变，仅优化视觉呈现

#### 影响范围
- 修改文件：`IdeaBox/Views/WeekCalendarView.swift`
- 代码变更：5 行
- 性能影响：无

---

## [1.1.0] - 2025-10-01

### 🎉 重大更新：iOS 风格周视图日历

#### 新增功能 ✨

##### 按周翻页导航
- ✅ **iOS 原生分页体验**：采用 TabView 实现流畅的周视图翻页
- ✅ **左右滑动翻周**：向左查看下一周，向右查看上一周
- ✅ **智能星期保持**：翻周后自动选中新周的同一星期几
  - 例如：从本周三滑到下周，自动选中下周三
- ✅ **边界限制**：可查看前后各52周（共105周），到达边界时提供弹性反馈
- ✅ **流畅动画**：约300ms的翻页动画，符合iOS HIG标准

##### "今天"按钮智能显示
- ✅ **条件渲染**：选中今天时自动隐藏，选中其他日期时自动显示
- ✅ **快速跳转**：点击"今天"按钮立即回到今天所在周
- ✅ **平滑过渡**：显示/隐藏带有200ms淡入淡出动画

##### 数据模型增强
- ✅ **新增 WeekData 模型**：完整的周数据结构（周一到周日）
- ✅ **DayItem 增强**：添加 `weekdayIndex` 属性（1-7，1=周一）
- ✅ **周计算工具**：DateHelper 新增4个周相关计算方法

#### 技术改进 🛠️

##### 架构升级
- **分页机制**：从自由滚动改为 TabView 分页模式
- **状态管理**：简化状态层级，周偏移量由 WeekCalendarView 内部管理
- **性能优化**：TabView 懒加载，仅保持当前+前后各1周在内存

##### 核心实现
- `WeekCalendarView.swift`：完全重构，实现 TabView 分页
- `BottomNavigationBar.swift`：实现 TodayButtonController 契约
- `DateHelper.swift`：新增周数据生成和计算方法
  - `getMonday(for:)` - 获取周一日期
  - `getWeek(offset:relativeTo:)` - 生成周数据
  - `weekOffset(for:relativeTo:)` - 计算周偏移量
  - `dateInWeek(_:weekday:)` - 获取周内特定日期

##### 测试覆盖（TDD）
- ✅ 44个单元测试用例已编写
- ✅ 3个接口契约测试套件
- ✅ 完整的测试目录结构（Models/Helpers/UI）

#### 边界情况处理 🔒
- ✅ 跨月翻周：正确处理月份边界（如3月31日→4月6日）
- ✅ 跨年翻周：正确处理年份边界（如12月29日→1月4日）
- ✅ 闰年支持：自动识别闰年2月
- ✅ 时区感知：使用 Calendar API 确保时区正确

#### 破坏性变更 ⚠️
- `WeekCalendarView` 接口变更：
  - 移除：`days: [DayItem]` 参数（内部管理）
  - 移除：`onDateSelected` 回调（使用 Binding）
- `BottomNavigationBar` 接口变更：
  - 新增：`selectedDate: Binding<Date>` 参数
- `ContentView` 简化：
  - 移除：`weekDays` 状态
  - 移除：`updateWeekDays()` 方法

---

## [1.0.0] - 2025-10-01

### Bug 修复 🐛
- ✅ 修复时间段高度问题 - 现在会根据该时段内的事件数量动态调整高度
- ✅ 删除了针对特定时间（12:55）的硬编码特殊逻辑
- ✅ 优化事件卡片显示 - 统一样式，不再有特殊情况
- ✅ **重构时间轴架构** - 修复左右对齐问题
  - 采用整体式结构，每个时间段（时间+事件）作为一个完整单元
  - 从根本上解决了时间标签和事件列表不对齐的问题
  - 新增 `TimeSlotRow` 组件统一管理每个时间段

### 新增功能

#### 日期选择功能 ✨
- ✅ **横向滚动日历**：可以左右滑动查看前后120天的日期
- ✅ 点击周日历中的日期可以切换查看
- ✅ 选中日期会以红色背景高亮显示
- ✅ 自动滚动定位：点击日期或切换时自动滚动到选中位置
- ✅ 日期标题会自动更新为选中的日期
- ✅ 显示对应的公历和农历信息
- ✅ 底部"今天"按钮可快速返回当天

#### 全天时间轴 ⏰
- ✅ 完整24小时时间轴（00:00-23:00）
- ✅ 流畅的上下滚动功能
- ✅ **智能高度调整**：每小时的高度根据事件数量自动调整
  - 没有事件：最小高度60px
  - 有事件：自动扩展以容纳所有事件（每个事件72px + 8px间距）
- ✅ 事件按时间自动排列，同一小时内的事件垂直堆叠
- ✅ 自动滚动到早上8点，最佳初始视图
- ✅ 时间网格线辅助视觉定位
- ✅ 滚动指示器显示当前位置

#### 核心功能
- ✅ 周日历视图展示
- ✅ 时间轴事件列表
- ✅ 多种事件类型支持（链接、文本日记、语音日记）
- ✅ 底部导航栏

### 技术改进

- 新增 `DateHelper` 工具类处理日期相关逻辑
- 优化 `DayItem` 模型，添加 `date` 属性和 `Equatable` 协议
- 改进状态管理，使用 `@State` 和 `@Binding` 实现日期选择
- **架构重构**：
  - 从分离式结构（时间列 + 事件列）改为整体式结构
  - 新增 `TimeSlotRow` 组件，将每个时间段作为独立单元
  - 自动对齐，无需手动计算左右高度
- 简化 `EventItem` 模型，移除不再需要的 `yPosition` 属性
- 统一事件卡片样式，移除硬编码的特殊逻辑
- 所有视图组件支持动态日期更新

### 项目结构

```
IdeaBox/
├── Models/          # 数据模型
├── Views/           # UI组件
├── Helpers/         # 工具类（新增）
└── Assets/          # 资源文件
```

---

## 未来计划

### v1.1.0
- [ ] 实现添加事件功能
- [ ] 事件数据持久化
- [ ] 滑动切换周视图

### v1.2.0
- [ ] 月视图支持
- [ ] 事件编辑和删除
- [ ] 搜索和筛选功能

### v1.3.0
- [ ] 云同步功能
- [ ] 多设备数据同步
- [ ] 分享功能

---

更多信息请查看 [README.md](README.md)

