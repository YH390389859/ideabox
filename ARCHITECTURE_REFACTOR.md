# 时间轴架构重构

## 旧架构的问题

### 分离式结构
```
HStack {
    VStack {  // 左边：所有时间标签
        12:00
        13:00
        14:00
    }
    
    VStack {  // 右边：所有事件容器
        [事件容器1]
        [事件容器2]
        [事件容器3]
    }
}
```

**问题**：
- 左右两边独立计算高度，容易不对齐
- 很难确保每个时间段的左右部分高度一致
- padding、spacing 很难统一

## 新架构

### 整体式结构
```
VStack {
    TimeSlotRow {  // 12:00 时段（整体）
        HStack {
            12:00（时间）
            [事件容器]
        }
    }
    
    TimeSlotRow {  // 13:00 时段（整体）
        HStack {
            13:00（时间）
            [事件容器]
        }
    }
    
    TimeSlotRow {  // 14:00 时段（整体）
        HStack {
            14:00（时间）
            [事件容器]
        }
    }
}
```

**优势**：
- ✅ 每个时间段是一个整体单元
- ✅ 左边时间和右边事件天然对齐
- ✅ 高度由整个 Row 统一计算
- ✅ 逻辑清晰，易于维护

## 核心组件

### TimeSlotRow
```swift
struct TimeSlotRow: View {
    let hour: Int           // 时段（0-23）
    let timeLabel: String   // 时间标签（如 "12:00"）
    let events: [EventItem] // 该时段的事件列表
    
    var body: some View {
        HStack(alignment: .top) {
            // 左：时间标签
            Text(timeLabel)
                .frame(width: 60)
            
            // 右：事件列表（垂直排列）
            VStack(spacing: 8) {
                ForEach(events) { event in
                    EventCard(event: event)
                }
            }
        }
        .frame(height: 根据事件数量计算)
    }
}
```

## 高度计算逻辑

```swift
private var rowHeight: CGFloat {
    if events.isEmpty {
        return 60px  // 最小高度
    }
    
    // 有事件：
    // = 事件总高度 + 间距 + padding
    return count × 72 + (count-1) × 8 + 20
}
```

### 示例

| 事件数量 | 高度计算 | 结果 |
|---------|---------|------|
| 0 | 60px | 60px |
| 1 | 72 + 0×8 + 20 | 92px |
| 2 | 144 + 8 + 20 | 172px |
| 3 | 216 + 16 + 20 | 252px |

## 对齐效果

```
┌─────────────────────────────────────┐
│ 12:00 │                             │
│       │  [12:30 事件1]              │  ← 整体高度 172px
│       │  [12:55 事件2]              │
├─────────────────────────────────────┤
│ 13:00 │                             │  ← 整体高度 60px
├─────────────────────────────────────┤
│ 14:00 │                             │
│       │  [14:20 事件]               │  ← 整体高度 92px
└─────────────────────────────────────┘
```

**完美对齐**：
- ✅ 时间标签和事件容器在同一行
- ✅ 分隔线完全水平
- ✅ 不会出现错位

## 数据流

```
TimelineView
    ↓ 传入所有事件
eventsForHour(12)
    ↓ 筛选出 12:00-12:59 的事件
TimeSlotRow
    ↓ 计算该时段高度
    ↓ 渲染时间 + 事件
完美对齐的一行！
```

## 总结

新架构通过将每个时间段作为**独立的整体单元**，从根本上解决了对齐问题。

不再需要：
- ❌ 分别计算左右高度
- ❌ 手动同步 padding
- ❌ 复杂的对齐逻辑

只需要：
- ✅ 一个 TimeSlotRow 组件
- ✅ 自动计算整体高度
- ✅ 天然对齐

